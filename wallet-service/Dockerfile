# Wallet Service - placeholder até implementação
# Porta: 3001
FROM node:20-alpine
WORKDIR /app
EXPOSE 3001
# Mantém container vivo; substituir por CMD ["node", "dist/index.js"] quando houver build
CMD ["node", "-e", "setInterval(()=>{}, 1e6)"]
