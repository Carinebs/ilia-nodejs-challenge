# Frontend - placeholder até implementação (React + Vite)
# Porta: 5173
FROM node:20-alpine
WORKDIR /app
EXPOSE 5173
# Mantém container vivo; substituir por npm run dev quando houver projeto Vite
CMD ["node", "-e", "setInterval(()=>{}, 1e6)"]
